@model LoginFacebookGoogle.Models.IndexViewModel

@{
    ViewBag.Title = "Gestionar";
}




<html>
<head>

    <link href="~/Content/estilos.css" rel="stylesheet" @*id="bootstrap-css"*@>
    <link href="~/Content/Estilos.css" rel="stylesheet" @*id="bootstrap-css"*@>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

</head>
<body>

    @*@Html.Partial("Actualiza")*@

    @*@using (Html.BeginForm())
        {
            <div class="container">
                <div class="abs-center2">
                    <section id="loginForm">

                        <h2>Actualizar datos</h2>



                        <div class="form-group">
                            @Html.LabelFor(m => m.PhoneNumber, new { @class = "col-md-2 control-label" })
                            @Html.TextBoxFor(m => m.PhoneNumber, new { @class = "form-control" })
                        </div>

                        <div class="form-group">
                            <div class="col-md-offset-2 col-md-10">
                                <input type="Enviar" class="btn btn-default" value="Registrarse" />
                            </div>
                        </div>
                    </section>
                </div>
            </div>
        }*@
</body>
</html>




@*<dl class="dl-horizontal">
        <dt>Contraseña:</dt>
        <dd>
            [
            @if (Model.HasPassword)
            {
                @Html.ActionLink("Cambiar la contraseña", "ChangePassword")
            }
            else
            {
                @Html.ActionLink("Crear", "SetPassword")
            }
            ]
        </dd>
        <dt>Inicios de sesión externos:</dt>
        <dd>
            @Model.Logins.Count [
            @Html.ActionLink("Administrar", "ManageLogins") ]
        </dd>


        </dd>
    </dl>*@

